# Task

Users report that `range(1, 5)` returns `[1, 2, 3, 4]` but the documented
contract says the end is inclusive. The test suite currently fails. Find the
root cause and fix it. Do not weaken or delete any test.
